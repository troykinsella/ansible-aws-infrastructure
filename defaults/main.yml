---
#aws_project: default
aws_environment_name: production
aws_region: us-west-2
aws_preferred_availability_zone: a

aws_vpc_tenancy: default
    # or dedicated

#aws_key_pair_name: admin

aws_subnet_prefix: "10.0"
#aws_subnets:
#  - cidr: "{{ aws_subnet_prefix }}.0.0/27"
#    az: "{{ aws_region }}a"
#    resource_tags:
#      'Project': "{{ project }}"
#      'Role':    'management'
#      'Name':    "{{ project }}-management-a"

#aws_security_group:
#  inbound:
#    description: "Inbound Proxy"
#    rules:
#    - { proto: tcp, from_port: 80,    to_port: 80,    cidr_ip: 0.0.0.0/0 }
#    - { proto: tcp, from_port: 443,   to_port: 443,   cidr_ip: 0.0.0.0/0 }

aws_ec2_host_group: ec2hosts

aws_ami_id: ami-9abea4fb

aws_root_volume_size: 10
